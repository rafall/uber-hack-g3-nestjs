version: '3.3'

services:
   db:
     image: mysql:latest
     volumes:
       - db_data:/var/lib/mysql
     restart: always
     environment:
       MYSQL_ROOT_PASSWORD: root
       MYSQL_DATABASE: uberhack
       MYSQL_USER: uberhack
       MYSQL_PASSWORD: uberhack
volumes:
    db_data: {}